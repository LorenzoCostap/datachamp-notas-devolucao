name: Validar documentação

on:
  push:
    branches: [ main ]

jobs:
  validar:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verificar README
        run: |
          test -f README.md
          test -s README.md
